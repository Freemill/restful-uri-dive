spring:
  application:
    name=my-restful-service

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  messages:
    basename: messages

server:
  port: 8088

logging:
  level:
    org.springframework: debug

management:
  endpoints:
    web:
      exposure:
        include: "*"

